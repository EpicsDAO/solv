export enum LANG {
  EN = 'en',
  JA = 'ja',
}

export enum Network {
  MAINNET = 'mainnet',
  // DEVNET = 'devnet',
  TESTNET = 'testnet',
}

export enum NodeType {
  VALIDATOR = 'validator',
  RPC = 'rpc',
}

export enum ValidatorType {
  SOLANA = 'solana',
  AGAVE = 'agave',
  JITO = 'jito',
  FIREDANCER = 'firedancer',
}

export enum RpcType {
  SOLANA = 'solana',
  AGAVE = 'agave',
  JITO = 'jito',
  GEYSER_GRPC = 'geyser-grpc',
  DAS_API = 'das-api',
}

export enum MNT_DISK_TYPE {
  SINGLE = 'single',
  DOUBLE = 'double',
}
